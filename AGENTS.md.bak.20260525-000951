# AGENTS.md

## 專案總覽

本專案是資料庫管理期末專題的「演唱會訂票系統」網站，主要在 MAMP 環境中以 PHP 頁面呈現。現階段重點是前端網頁設計，包含首頁演唱會廣告輪播、近期演唱會購票卡片、會員登入入口，以及後續可延伸的演唱會詳情、會員資訊、後台管理等頁面。

目前資料多半先寫在 PHP 陣列中，之後可改成從 MySQL 資料庫查詢後輸出到同一份 HTML 結構。

## 技術棧

- PHP：頁面檔案與未來資料庫串接。
- HTML：頁面結構。
- CSS：視覺設計與響應式版面，主要檔案為 `assets/css/style.css`。
- JavaScript：首頁輪播互動，主要檔案為 `assets/js/homepage.js`。
- MAMP：本機 Apache/PHP/MySQL 開發環境。
- MySQL：後續資料庫課程專題串接使用。

## 啟動方式

1. 開啟 MAMP。
2. 啟動 Apache 與 MySQL。
3. 確認專案位於：

```text
/Applications/MAMP/htdocs/concert_system
```

4. 在瀏覽器開啟：

```text
http://localhost:8888/concert_system/index.php
```

如果 MAMP Apache port 設為 80，則使用：

```text
http://localhost/concert_system/index.php
```

## 測試與檢查

目前尚未設定自動化測試。

修改前端時至少檢查：

- 首頁是否能正常開啟。
- 輪播是否會自動切換。
- 左右切換按鈕是否可用。
- 輪播圓點是否可切換。
- 三張海報是否完整顯示在 16:9 區塊中，不能被裁切。
- 下方演唱會購票卡片是否與上方輪播資料一致。
- 手機寬度下文字與按鈕不能重疊或被裁切。

## 目錄結構

- `index.php`：首頁，目前包含三場演唱會資料、輪播廣告與購票卡片。
- `assets/css/style.css`：全站主要樣式。
- `assets/js/homepage.js`：首頁輪播邏輯。
- `assets/images/`：首頁輪播海報圖片，建議命名為 `concert-1.png`、`concert-2.jpg`、`concert-3.jpg`。
- `customer/login.php`：會員登入頁，目前為占位頁。
- `customer/member.php`：會員資訊頁，目前為占位頁。
- `customer/concert_detail.php`：演唱會詳細資訊與訂票資訊頁，目前為占位頁。
- `customer/register.php`：會員註冊頁，目前尚待設計。
- `manager/dashboard.php`：管理者後台頁，目前尚待設計。
- `includes/db.example.php`：未來資料庫連線範例。
- `sql/schema.sql`：未來資料庫 schema。

## 前端設計方向

- 首頁上方保留清楚的導覽列與會員登入按鈕。
- 輪播區使用固定 16:9 海報空間，圖片需完整顯示，不要裁切海報內容。
- 演唱會名稱、日期、地點、查看詳情按鈕放在海報下方資訊列，避免遮住海報。
- 下方購票卡片簡潔顯示演唱會資訊，點擊可進入詳情 / 訂票頁。
- 首頁三場演唱會資料應集中在 `index.php` 的 `$concerts` 陣列，避免上方輪播與下方卡片資料不同步。
- 設計以期末展示為目標，優先保持畫面清楚、流程直覺、資料庫欄位概念明確。

## 程式風格

- PHP 頁面中輸出文字時，使用 `htmlspecialchars()` 避免 HTML 注入。
- CSS 儘量維持集中管理，不在 HTML 中寫 inline style。
- JavaScript 只負責互動效果，不在裡面硬寫演唱會資料。
- 圖片路徑使用專案相對路徑，例如 `assets/images/concert-1.png`。
- 修改頁面時保持檔案責任清楚，不要把登入、詳情、後台功能全部塞進首頁。

## 部署備註

目前以本機 MAMP 展示為主，尚未設定正式部署流程。

若要移到其他電腦展示，需確認：

- 專案資料夾放在 MAMP 的 `htdocs`。
- 圖片檔案有一起複製。
- MAMP port 設定與展示網址一致。
- 若已串接資料庫，需匯入 `sql/schema.sql` 並設定資料庫連線檔。

## 給 AI Coding Agents 的指示

- 先閱讀本檔案與目前相關 PHP/CSS/JS，再修改。
- 除非使用者明確要求，不要自行重構整個專案。
- 若使用者只要求設計某一頁，修改範圍應限制在該頁與必要的 CSS/JS。
- 不要移除使用者提供的圖片或覆蓋其檔名。
- 首頁演唱會資料應維持單一來源 `$concerts`，避免輪播與購票卡片不同步。
- 若新增頁面，優先使用既有視覺語言與資料庫專題需求。
- 完成後告知使用者修改了哪些檔案，以及如何在 MAMP 中查看。
